import { Layout } from '../components/Layout';
import { Search as SearchComponet } from '../components/Search';

export const Search = () => {
  return (
    <Layout title="open Search">
      <SearchComponet />
    </Layout>
  );
};
